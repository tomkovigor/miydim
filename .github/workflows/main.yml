- имя: Настройка среды Node.js использует: actions/setup-node@v4.0.2 с: # Установите всегда-auth в npmrc. always-auth: # необязательно, по умолчанию false # Спецификация версии используемой версии. Примеры: 12.x, 10.15.1, >=10.15.0. версия узла: # необязательно # Файл, содержащий спецификацию версии используемой версии. Примеры: package.json, .nvmrc, .node-version, .tool-versions. node-version-file: # необязательный # Целевая архитектура для использования Node. Примеры: x86, x64. Будет использовать системную архитектуру по умолчанию. архитектура: # необязательно # Установите эту опцию, если вы хотите, чтобы действие проверило последнюю доступную версию, которая удовлетворяет спецификации версии. check-latest: # необязательный # Дополнительный реестр для настройки для auth. Установит реестр в файле .npmrc и .yarnrc уровня проекта и настроит auth для чтения из env.NODE_AUTH_TOKEN. registry-url: # необязательно # Необязательная область для аутентификации в реестрах с областью действия. Будет возвращаться к владельцу репозитория при использовании реестра пакетов GitHub (https://npm.pkg.github.com/). область применения: # необязательный # Используется для извлечения дистрибутивов узлов из версий узлов. Поскольку существует значение по умолчанию, это обычно не предоставляется пользователем. При запуске этого действия на github.com достаточно значения по умолчанию. При работе на GHES вы можете передать личный токен доступа для github.com, если вы испытываете ограничение скорости. token: # необязательно, по умолчанию ${{ github.server_url == 'https://github.com' && github.token || '' }} # Используется для указания менеджера пакетов для кэширования в каталоге по умолчанию. Поддерживаемые значения: npm, yarn, pnpm. кэш: # необязательно # Используется для указания пути к файлу зависимостей: package-lock.json, yarn.lock и т. д. Поддерживает подстановочные знаки или список имен файлов для кэширования нескольких зависимостей. cache-dependency-path: # необязательный 
